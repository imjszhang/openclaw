# 金字塔全树

> 所属视角：（填写视角名称）

从 [scqa.md](../scqa.md) 的塔尖结论出发，逐层向下展开。

## 塔尖

> 插件开发生命周期必须严格遵循"SDK 隔离 + 清单驱动 + 安全加载”规范，通过 jiti 动态加载配合严格的路径/所有权检查，构建运行时安全防线。

## Key Line（顶层论点）

塔尖下面的第一层支撑论点。每个论点回答读者看到塔尖后的下一层追问。

| 序号 | 论点                                                                             | 逻辑顺序类型 | 引用 Groups | 详细展开                            |
| ---- | -------------------------------------------------------------------------------- | ------------ | ----------- | ----------------------------------- |
| KL01 | 插件开发必须实施 SDK 隔离与清单驱动，从源头界定扩展边界与依赖范围                | 结构         | G32         | kl01-plugin-sdk-isolation.md        |
| KL02 | 运行时加载需采用 jiti 动态执行配合严格的路径解析，确保代码仅在受控上下文中激活   | 时间         | G32         | kl02-jiti-dynamic-loading.md        |
| KL03 | 安全防线依赖于所有权检查与执行审批机制，强制阻断未授权的文件访问与高风险命令注入 | 程度         | G32, G39    | kl03-ownership-and-exec-security.md |

每个 Key Line 在本目录下有独立文件，承载该支的逐层展开。

## Key Line 文件模板

```markdown
# KL01: [论点观点句]

> 所属视角：...
> 上层论点：塔尖

## 支撑论点

### 1.1: [观点句]

- 逻辑顺序：时间/结构/程度
- 引用 atoms: ...
- 引用 groups: ...

### 1.2: [观点句]

（以此类推，可继续向下展开子层级）
```

## 结构检查

- [ ] 每个子层是否回答了上层引发的"为什么"或"怎么做"（纵向逻辑）
- [ ] 同层论点之间的逻辑顺序是否统一且合理
- [ ] 每个叶子节点是否都有 atom 引用支撑

---

## 修订记录

| 日期 | 变更摘要               |
| ---- | ---------------------- |
|      | （首次构建后开始记录） |
