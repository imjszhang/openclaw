---
summary: "P11 browser relay：如何设计一种架构，既能复用用户现有浏览器环境以保留登录态，又能避免独立实例的资源开销，同时确保 Agent 对标签页的控制是安全、可控且非侵入式的？"
read_when:
  - 需要了解 P11 browser relay 的核心论点和结构时
  - 需要判断是否深入阅读该视角的完整内容时
---

# P11 browser relay

## 读者

系统架构师、安全工程师、AI Agent 开发者 | 核心诉求：如何在保障安全可控的前提下，以最低资源成本实现 AI Agent 对用户浏览器标签页的精准共享与控制

## SCQA 摘要

- **S**: 在构建 AI Agent 自动化工作流时，浏览器是执行网页交互任务的核心载体。当前主流方案依赖独立启动的沙箱浏览器实例或无头浏览器，以确保任务隔离与环境纯净。
- **C**: 然而，独立实例不仅消耗巨大的系统资源（内存/CPU），更因环境隔离导致无法复用用户日常浏览器中已建立的登录态（Cookies/Session），迫使 Agent 重复执行繁琐的登录验证流程，严重降低了自动化效率与用户体验。
- **Q**: 如何设计一种架构，既能复用用户现有浏览器环境以保留登录态，又能避免独立实例的资源开销，同时确保 Agent 对标签页的控制是安全、可控且非侵入式的？
- **A**: Browser Relay 架构通过“本地中继服务器 + 扩展徽章状态机”实现安全可控的浏览器标签页共享控制，在避免独立浏览器实例资源开销的同时，填补了沙箱环境在“带登录态交互”场景的能力空白。

## Key Lines

| 序号 | 论点                                                                                      | 引用 Groups | 展开文件                       |
| ---- | ----------------------------------------------------------------------------------------- | ----------- | ------------------------------ |
| KL01 | Browser Relay 架构通过“本地中继服务器 + 扩展徽章状态机”实现安全可控的浏览器标签页共享控制 | G20, BR     | browser-relay-architecture.md  |
| KL02 | 该架构在避免独立浏览器实例资源开销的同时，显著降低了系统运行成本                          | G20, BR     | resource-efficiency-benefit.md |
| KL03 | 方案有效填补了沙箱环境在“带登录态交互”场景下的能力空白                                    | G34, JE     | sandbox-login-gap-solution.md  |
| KL04 | 通过复用用户日常浏览器环境，支持多浏览器协同与远程控制需求                                | G34, JE     | multi-browser-collaboration.md |

## 深入阅读

- SCQA 完整设计：[scqa.md](scqa.md)
- 金字塔全树：[tree/README.md](tree/README.md)
- MECE 验证：[validation.md](validation.md)
